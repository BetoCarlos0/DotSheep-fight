version: '3.4'
container_name: sheepfightlocal
services:
  sheepfightlocal:
    image: ${DOCKER_REGISTRY-}dotsheepfightlocal
    build:
      context: .
      dockerfile: .
    ports:
      - "8080:80"
    depends_on:
      - sqlserver

  sqlserver:
    image: postgres
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=roberto123
    volumes:
      - D:\betoc\Documents\vs-projetos\GitHub\DotSheep-fight:/var/opt/mssql/data
    ports:
      - "1450:1433"
